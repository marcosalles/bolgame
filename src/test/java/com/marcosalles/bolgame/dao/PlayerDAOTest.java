package com.marcosalles.bolgame.dao;

import org.junit.jupiter.api.Test;

import static org.hamcrest.MatcherAssert.assertThat;

class PlayerDAOTest {

	@Test
	void component__should_not_need_testing() {
		assertThat("This component does not have any extra implementations so the test suite from springframework should suffice.", true);
	}
}